# Dev Tools - Part 2

1. num 1 and num 2 were both strings and when passed into `calculateSum`, the `+` concatenated them instead of adding them
2. We can fix it by parsing the values for num1 and num2 as numbers before using them
